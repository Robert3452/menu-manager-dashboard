{
    "configurations": [
        {
            "type": "chrome",
            "url":"http://localhost:3000",
            "name": "Run Script NEXT DASHBOARD: dev",
            "request": "launch",
            // "command": "npm run dev",
            "cwd": "${workspaceFolder}/src"
        }
    ]
}